#include<iostream>

using namespace std;

int main() {
    int a, b, c, n;
    cin >> a >> b >> c >> n;
    
    int x = a + b - c;
    if(x > n-1) cout << -1;
    else if(a < c) cout << -1;
    else if(b < c) cout << -1;
    else cout << n - x;
    return 0;
}
